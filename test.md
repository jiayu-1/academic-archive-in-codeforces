
### 关键语法说明
- 文字直接写，空行表示分段。
- **代码块**：用三个反引号 **```** 包裹，后面跟上语言名（如 `cpp`、`python`、`java`），可以高亮。
- 标题用 `#`（一级标题）、`##`（二级标题）。
- 链接写法：`[显示文字](网址)`。
- 列表用 `-` 或 `1.`。

## 📂 保存文件
写完内容后，滚动到页面底部，填写提交说明（比如 “add solution for CF 1000A”），然后点击 **“Commit changes”**。

## 💡 如果你想要代码和文字混合在同一个文件里还能运行？
那不可能，因为代码文件（如 `.cpp`）里不能写大段中文解释（会被当作注释或报错）。  
所以**题解的标准做法就是 Markdown**，把代码贴进去就行。如果你希望别人能直接复制代码跑，可以把纯代码文件（`sol.cpp`）也放在同一目录下，然后在 `.md` 里引用它。

## 🖼️ 截图里提示“Enter file contents here”
没错，就在那个大框里按照上面的 Markdown 格式写内容。

## ✅ 总结
- 新建 `.md` 文件
- 用 ` ```cpp ` 等代码块贴代码
- 用普通文字写思路
- 保存提交

这样你的仓库就会呈现出一份漂亮的题解文档，GitHub 会自动渲染排版。如果需要我帮你写一个具体题目的示例，可以把题目告诉我。
